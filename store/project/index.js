
const state = {
	listType: ''
}

const getters = {}

const actions = {}

const mutations = {
	setListType(s, o) {
		s.listType = o
	}
}

export default {
    namespaced: true,
    state,
    getters,
    actions,
    mutations
}
