
const state = {
	selEmployee: {}
}

const getters = {}

const actions = {}

const mutations = {
	setSelEmployee(s, o) {
		s.selEmployee = o
	}
}

export default {
    namespaced: true,
    state,
    getters,
    actions,
    mutations
}
